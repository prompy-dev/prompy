# import os
# from langchain.utilities import SQLDatabase

# db = SQLDatabase.from_uri(os.environ("SUPABASE_URI"))

'''
  @wip - working though how to do this in a non-blocking way
'''
def db_query(user_query: str):
  pass
